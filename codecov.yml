# https://docs.codecov.io/docs/codecov-yaml

coverage:
  precision: 2
  round: down
  range: "70...100"

  status:
    project:
      default:
        target: auto

    patch:
      default:
        target: 80

    changes: false

comment: false

ignore:
  - ".*_charlatan.go"
